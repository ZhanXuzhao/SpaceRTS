# space001

这是一个 Godot 项目，包含飞船、武器、蒙版和最小地图等功能。

## 目录结构

- `assets/` - 资源导入文件
- `scenes/` - Godot 场景文件
- `scripts/` - Godot 脚本文件

## 使用说明

1. 打开 `project.godot`。
2. 在 Godot 编辑器中运行 `main.tscn`。

## 备注

- `.gd.uid` 文件由 Godot 自动生成并维护。
- 只需编辑 `.gd` 脚本，不要随意删除 `.gd.uid`。